char characterName[] = "John"; - zmienna znakowa, [] daje znać kompilatorowi że chcemy przechować więcej znaków niż 1
char characterName = 'A'; - zmienna znakowane, w której może zawrzeć jeden znak
int characterAge = 35; - zmienna liczbowa, przechowuje liczbę całkowitą
double gpa = 3.7 - zmienna zmiennoprzecinkowa
printf("%s", characterName) - wyświetla na ekranie wartość zmiennej characterName
scanf("%d", &age); - przekazywanie znaków z klawiatury do programu (musi być wskaźnik przed każdą zmienną)
fgets("%s", name, stdin); - przekazywanie znaków z pliku do programu, stdin = standardowe dane wyjściowe
%s - dajemy znać kompilatorowi że w tekście chcemy zawrzeć ciąg znaków
%c - dajemy znać kompilatorowi że w tekściemy chcemy zawrzeć jeden znak
$d - dajemy znać kompilatorowi że w tekście chcemy zawrzeć liczbę całkowitą
%f - dajemy znać kompilatorowi że w tekście chcemy zawrzeć liczbe zmiennoprzecinkową
%p - dajemy znać kompilatorowi że w tekście chcemy zawrzeć miejsce w pamięci
%lf - funkcja scanf, czyta całą liczbe zmiennoprzecinkową na wejściu i na wyjściu
pow(5,3) - podniesie 5 do potęgi 3
sqrt(36) - pierwiastek kwadratowy z 36
ceil(36.365) - zaokrągla liczbę do góry, czyli 37
floor(36.656) - zaokrągla liczbę do dołu, czyli 36
const int num = 5; - const przypisuje num liczbe stałą o wartości 5 (nie można jej zmienić)
void sayHi(char name[]){} - tworzenie funkcji, z parametrem char
sayHi("Mike"); - wstawianie funkcji do int main z parametrem char, której nadajemy wartość "Mike"
double cube(double num){return num * num * num;;} - tworzenie zmiennej z parametrami i podaną instrukcją w środku, która zwraca wartość podaną w printf poniżej
printf("Answer: %f", cube(3.0)); - wyświetlanie zmiennej z parametrami + dodajemy do niej wartość.
struct Student{}; - tworzenie struktury
struct Student student1; - wstawanie struktury do int main z nadaniem nowego członu czyli student1
student1.age = 22; - nadanie parametru age o wartosci 22 dla student1 w int main
strcpy(student1.name, "Jim"); - nadanie wartości dla char name o wartości "Jim" dla student1 w int main
int nums[3][2] = {{1,2},{3,4}}; - tworzenie tablicy dwuwymiarowej
*pAge - wskaźnik
&age - nadanie adresu pamięci
FILE *fpointer = fopen("employees.txt", "w"); - Operacje na pliku, *fpointer = nazwa pliku w programie, fopen = otwarcie pliku, "empleyees.txt" - nazwa pliku
("w"); - Operacje na pliku, otwarcie pliku do zapisu
("a"); - Operacje na pliku, otwarcie pliku w celu dopisania na koniec pliku
("r"); - Operacje na pliku, otwarcie pliku w trybie tekstowym do odczytu
fprintf(fpointer, "\nXDDD"); - Operacje na pliku, odczyt lub nadpis do pliku jakiegoś tekstu, f dodajemy na operacjach z plikami, fpointer = nazwa i miejsce pliku w programie
fclose(fpointer); - Operacje na pliku, zamknięcie pliku (np. w celu zwolnienia pamięci)
char line[255]; - Operacje na pliku, poinformowanie programu żeby mógł przechować 255 znaków
fgets(line, 255, fpointer); - Operacje na pliku, pobieranie danych z pliku, (pamiec char, ilosc znaków, nazwa pliku w programie)
printf("%s", line); - Operacje na pliku, wyświetlanie danych z pliku ("line"!!!)
